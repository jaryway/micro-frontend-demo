{
  "name": "micro-frontend-demo",
  "version": "1.0.0",
  "description": "s",
  "main": "index.js",
  "scripts": {
    "build": "yarn build:base && yarn build:sub1app && yarn build:sub2app && yarn build:boot",
    "build1": "yarn build:base && yarn build:sub1app && yarn build:boot",
    "build:boot": "cd ./bootstrap && yarn merge:conf && yarn build",
    "build:base": "cd ./base-app && yarn build:micro",
    "build:sub1app": "cd ./sub1-app && yarn build:micro",
    "build:sub2app": "cd ./sub2-app && yarn build:micro"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jaryway/micro-frontend-demo.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/jaryway/micro-frontend-demo/issues"
  },
  "homepage": "https://github.com/jaryway/micro-frontend-demo#readme",
  "dependencies": {
    "hsp-utils": "^1.1.6"
  }
}
